---
import LandingLayout from '@layouts/LandingLayout.astro';
import Button from '@components/Button.astro';
import { useTranslations } from '@i18n/utils';
import type { Lang } from '@i18n/translations';

const lang: Lang = 'de';
const t = useTranslations(lang);
---

<LandingLayout title="Download bereit | Hustlrr" description="Dein Download ist bereit." lang={lang}>
  <div class="animate-hero">
    <div class="text-5xl mb-6" aria-hidden="true">&#x1F4E5;</div>
    <h1 class="text-[var(--text-h1)] font-bold font-[var(--font-display)] mb-4">
      {t('landing.dl.title')}
    </h1>
    <p class="text-text-secondary mb-8">
      {t('landing.dl.message')}
    </p>
    <Button href="/de" variant="secondary" size="lg">
      Zurück zur Startseite
    </Button>
  </div>
</LandingLayout>
